{
  "name": "namada-sdk-tutorial",
  "private": true,
  "workspaces": [
    "app",
    "namada/*"
  ],
  "scripts": {
    "start": "cd app && yarn run start",
    "android": "cd app && yarn run android",
    "iOS": "cd app && yarn run ios"
  },
  "dependencies": {
    "@namada/shared": "^0.2.1"
  }
}
